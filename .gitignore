# Python
__pycache__/
*.pyc
.venv/
venv/

# Intermediate cross-session state (regenerated by the runner); the committed
# transcripts + score JSONs in eval_results/ are kept as reference results.
eval_results/memory_state.json

# Secrets
.openai_key
.env

# Scratch / experiments
*.log
